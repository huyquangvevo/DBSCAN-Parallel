#include<vector>

using namespace std;

int main(){
    vector<vector<int>> c;
    vector<int> p;
    p.push_back(0);
    p.push_back(2);
    p.push_back(4);
    p.push_back(3);
    c.push_back(p);
    return 0;
}